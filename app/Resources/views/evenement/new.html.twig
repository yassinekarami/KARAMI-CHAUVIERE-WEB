{% extends 'base.html.twig' %}

{% block title %}<h2>{{ 'index.add_event_trad' | trans }}</h2>{% endblock title %}

{% block traduction %}
    {% if app.request.locale == "en" %}
        <li><a href="{{ path('evenement_new' , {'_locale' : 'fr'}) }}">FR</a></li>
    {% else %}
        <li><a href="{{ path('evenement_new' , {'_locale' : 'en'}) }}">EN</a></li>
    {% endif %}
{% endblock %}

 {% block topButton %}
     <ul class="actions">
         <li><a href="{{ path('evenement_homepage') }}" class="button special big">Accueil</a></li>
     </ul>
 {% endblock topButton %}


{% block body %}


    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Create" />
    {{ form_end(form) }}

    {#
    <ul>
        <li>
            <a href="{{ path('evenement_homepage') }}">Back to the list</a>
        </li>
    </ul>
    #}

{% endblock %}


{% block rss %}

{% endblock %}