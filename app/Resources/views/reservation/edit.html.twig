{% extends 'base.html.twig' %}

{% block title %}<h2>{{ 'Edit' }}</h2>{% endblock title %}

{% block traduction %}
    {% if app.request.locale == "en" %}
        <li><a href="{{ path('reservation_edit' , {'_locale' : 'fr' , 'id': reservation.id }) }}">FR</a></li>
    {% else %}
        <li><a href="{{ path('reservation_edit' , {'_locale' : 'en' , 'id': reservation.id }) }}">EN</a></li>
    {% endif %}
{% endblock %}


{% block body %}
    <h1>Reservation edit</h1>

    {{ form_start(edit_form) }}
        {{ form_widget(edit_form) }}
        <input type="submit" value="Edit" />
    {{ form_end(edit_form) }}

    <ul>
        <li>
            <a href="{{ path('reservation_homepage') }}">Back to the list</a>
        </li>
        <li>

            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}


{% block rss %}

{% endblock %}