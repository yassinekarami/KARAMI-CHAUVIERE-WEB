{% trans_default_domain 'FOSUserBundle' %}

<div class="fos_user_user_show">

    {#
        <p>{{ 'profile.show.username'|trans }}: {{ user.username }}</p>
        <p>{{ 'profile.show.email'|trans }}: {{ user.email }}</p>

        <form class="contact100-form validate-form" action="{{ path("fos_user_change_password") }}" method="post">
            <input class="contact100-form-btn" type="submit" id="_submit" name="_submit" value="{{ 'Change Password'|trans }}" />
        </form>
    #}

    <h2>Reservation effectué</h2>




</div>
